pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo $PATH; /Applications/Docker.app/Contents/Resources/bin/docker'
            }
        }
    }
}
